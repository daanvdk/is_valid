sudo: false
language: python
python:
    - "3.4"
    - "3.5"
cache:
    pip: true
install:
    - python3 setup.py install
    - pip install flake8 codecov coverage
script:
    - flake8 is_valid
    - coverage run --include="is_valid/*" setup.py test
after_success:
    - codecov
